/// <reference types="react" />
import * as Types from './types/types';
export declare const getTags: ({ tags, bgColor, size, fallbackColor, minContrast }: {
    tags: Types.IconTag[];
    bgColor: string;
    size: number;
    fallbackColor: string;
    minContrast: number;
}) => Types.Tag[];
export declare const IconCloud: ({ id, tagCanvasOptions, tags, canvasContainerStyle: innerStyle, canvasHeight, canvasWidth, canvasStyle, fallbackHexColor, backgroundHexColor, iconSize: size, minContrastRatio: minContrast }: Types.IconCloudProps) => JSX.Element;
